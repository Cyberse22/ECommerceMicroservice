server:
  port: 9297

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Cyberse22/ECommerceMicroservice.git
          clone-on-start: true